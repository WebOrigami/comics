// Generate a audio narration file for each screenplay segment
Tree.map(data.ori, {
  extension: "->.mp3"
  value: narrate.js
})

// Cache the generated audio files in the "narration" folder
→ =Tree.cache(_, files:narration)
